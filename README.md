# Science Pupil's Book Standard Three ADT

Open `book.html` through a local web server to display and navigate the complete book. The former `reader.html` address redirects to the new reader so existing links continue to work.

Figure instructions are written to include both the image and its text description for screen-reader users.
